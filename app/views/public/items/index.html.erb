<div class="container">
  <div class="row">
    <div class="mx-auto">

      <div>ジャンル検索</div>

      <div>
        <div>
          <h5>商品一覧</h5>
          <span>(全<%= @item.count %>件)</span>
        </div>

        <% @item.each do |item| %>

          <% if item.image.attached? %>
            <%= image_tag item.image %>
        　<% else %>
            <%= image_tag 'no_image', size: "200x200" %>
          <% end %>

          <%= item.name %>
          <%= item.price %>

        <% end %>

        <%= paginate @items %>
      </div>


    </div>
  </div>
</div>